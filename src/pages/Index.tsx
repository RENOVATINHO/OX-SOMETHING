// ==============================
// Index.tsx — Componente de redirecionamento da rota raiz
// Re-exporta o LoginPage como componente padrão
// Garante que a rota "/" sempre renderize a tela de login
// ==============================

import LoginPage from "./LoginPage"; // Importa a página de login como componente principal
export default LoginPage; // Exporta diretamente — Index.tsx é sinônimo de LoginPage